# Mosquitto configuration for disposable per-test containers.
#
# The real-Mosquitto test harness (internal/testbroker) mounts this file and
# publishes a random loopback host port. Persistence is disabled so each
# container is disposable and no broker state leaks between tests.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
