# Mise passes worktree-specific listen addresses and storage as environment
# variables. NATS expands these variables when loading the config.
listen: $SIM_NATS_LISTEN
jetstream { store_dir: $SIM_NATS_STORE }
websocket { listen: $SIM_NATS_WS_LISTEN; no_tls: true }
http: $SIM_NATS_MONITOR_LISTEN
