# Manual deployment placeholders: use operator-managed NATS and MQTT endpoints.
# The real-device validation task generates its own worktree-local config.
adapter_id: zigbee2mqtt
nats_url: nats://127.0.0.1:4222
mqtt:
  url: tcp://127.0.0.1:1883
  base_topic: zigbee2mqtt
