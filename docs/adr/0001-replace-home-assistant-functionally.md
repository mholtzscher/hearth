# Replace Home Assistant functionally, not every protocol service

Hearthd will count as a Home Assistant replacement when it can provide the household's required controls, automations, schedules, alerts, history, and recovery behavior while Home Assistant is shut down. It may continue using specialist protocol services such as Zigbee2MQTT or Z-Wave JS because replacing Home Assistant does not justify reimplementing mature device protocol stacks.
