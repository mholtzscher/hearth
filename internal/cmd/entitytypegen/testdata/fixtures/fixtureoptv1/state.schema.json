{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:fixture:optional:v1:state",
  "title": "Fixture optional/v1 State",
  "type": "object",
  "additionalProperties": false,
  "required": ["code"],
  "properties": {
    "code": {"type": "integer", "minimum": 0, "maximum": 999},
    "note": {"type": "string"},
    "history": {"type": "array", "items": {"type": "integer", "minimum": 0, "maximum": 999}}
  }
}
